# jubilant-couscous

Foundation of the app. Duplicate this structure and build out from here.

## Structure

```
index.html   – Main HTML entry point
styles.css   – Base stylesheet
app.js       – JavaScript entry point
```

## Getting started

Open `index.html` in a browser to run the app locally.